<%= form_for(@saltydog_group) do |f| %>
  <% if @saltydog_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saltydog_group.errors.count, "error") %> prohibited this saltydog_group from being saved:</h2>

      <ul>
      <% @saltydog_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
